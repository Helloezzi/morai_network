
#pragma once

#include <iostream>
#include "socket.h"

namespace morainetwork {	
	/*
	bool Socket::OnClose() {
		#ifdef _WIN32
		if (::closesocket(socket) < 0) {
			return false;
		}
		WSACleanup();
		#else
		if (::close(socket) < 0) {
			return false;
		}
		#endif
		return true;
	}
	*/
}


